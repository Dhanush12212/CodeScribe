<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8" />

    <style> 
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: "Fira Code", monospace;
            padding: 20px;
            background: #fafafa;
            color: #000;
        }

        h2 {
            margin-bottom: 15px;
            font-family: Arial, Helvetica, sans-serif;
        }

        .meta {
            font-size: 13px;
            margin-bottom: 20px;
            color: #333;
            display:flex;
            justify-content: space-between;
        }

        pre {
            background: #1e1e1e;
            color: white;
            padding: 20px;
            border-radius: 10px;
            font-size: 13.5px;
            white-space: pre-wrap;
            word-break: break-word;
            overflow-x: hidden;
        }
 
        .hljs-keyword { color: #c678dd; }
        .hljs-string { color: #98c379; }
        .hljs-number { color: #d19a66; }
        .hljs-comment { color: #5c6370; font-style: italic; }
        .hljs-title { color: #61afef; }
        .hljs-meta { color: #e06c75; }
        .hljs-built_in { color: #56b6c2; }
    </style>
</head>

<body>
    <h2>CodeScribe Code</h2>

    <div class="meta">
        <div>
            <strong>Language:</strong> <%= language %><br>
        </div>
        <div>
            <strong>Generated On:</strong> <%= createdOn %>
        </div>
    </div>

    <pre><code class="hljs"><%- highlightedCode %></code></pre>
</body>
</html>
